// TODO: update to Dart 3.3 extension types when it's ready
// extension type Meters(int value) {
//   String get label => '${value}m';
//   Meters operator +(Meters other) => Meters(value + other.value);
// }

typedef UUID = String;
